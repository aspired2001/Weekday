// reducers.js

import { combineReducers } from 'redux';

// If you don't have a jobReducer, remove the import statement

const rootReducer = combineReducers({
    // If you don't have a jobReducer, you can remove this line
    // jobs: jobReducer,
    // Add more reducers here if needed
});

export default rootReducer;
